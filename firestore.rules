rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /SentiLensSessions/{sessionId} {
      allow read, write: if true; // In production, restrict to authenticated users
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
